# Data

Raw and processed datasets are generated locally and are not committed.

- `raw/`: unchanged source files
- `processed/`: reproducibly generated canonical tables and features

The next milestone will add explicit download, schema-validation, and cleaning
instructions.
